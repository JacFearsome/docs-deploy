# doc-deploy
### Launching the project
Launching the client:
```bash
cd client
npm install
npm start
```
Launching the server:
```bash
cd server
npm install
npm install -g nodemon
nodemon server.js
```